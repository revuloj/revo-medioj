#!/bin/bash

# vi povas doni kiel argumenton 'kreu-db', 'update-db' aŭ 'redaktantoj'

env_file=../../etc/.env
compose_file=docker-compose.yml

docker compose -f ${compose_file} --env-file ${env_file} exec -T cetonio bin/instalo.sh $@

